ORBIT:
  IMAGE_X: 0

COMBO_TREE:
  LIGHT:
    ATTACK: swingRegular
    THEN_NEXT:
      LIGHT:
        ATTACK: swingRegular
        THEN_NEXT:
          LIGHT:
            ATTACK: swingRegular

CONFIG:
  swingRegular:
    PROJECTILE_DAMAGE:
      - 10
